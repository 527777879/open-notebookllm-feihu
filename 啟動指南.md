# NoteBookLLM 啟動指南

## 環境需求

- **Python**: 3.10+
- **Node.js**: 18+
- **npm**: 9+
- **FFmpeg**: （可選）用於音檔處理

---

## 快速啟動

### 步驟 1：設定環境變數

```bash
# 複製環境變數範本
copy .env.example backend\.env
```

編輯 `backend\.env` 檔案，填入 API Key：

```env
# 選擇 AI Provider (gemini / openai / anthropic / ollama / groq / deepseek)
AI_PROVIDER=gemini

# ===== Gemini 設定 =====
GEMINI_API_KEY=你的-Gemini-API-Key
GEMINI_MODEL=gemini-2.0-flash
GEMINI_IMAGE_MODEL=gemini-2.0-flash-exp-image-generation

# ===== OpenAI 設定 =====
OPENAI_API_KEY=你的-OpenAI-API-Key
OPENAI_MODEL=gpt-5.1
OPENAI_IMAGE_MODEL=dall-e-3

# ===== Anthropic 設定 =====
ANTHROPIC_API_KEY=你的-Anthropic-API-Key
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ===== Ollama 設定（本地模型）=====
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# ===== Groq 設定（高速推理）=====
GROQ_API_KEY=你的-Groq-API-Key
GROQ_MODEL=llama-3.3-70b-versatile

# ===== DeepSeek 設定（推理模型）=====
DEEPSEEK_API_KEY=你的-DeepSeek-API-Key
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_REASONER_MODEL=deepseek-reasoner
```

**取得 API Key：**
- Gemini: https://aistudio.google.com/app/apikey
- OpenAI: https://platform.openai.com/api-keys
- Anthropic: https://console.anthropic.com/settings/keys
- Ollama: https://ollama.ai/download （本地安裝，無需 API Key）
- Groq: https://console.groq.com/keys
- DeepSeek: https://platform.deepseek.com/api_keys

---

### 步驟 2：建立虛擬環境並安裝後端依賴

> **重要**：強烈建議使用虛擬環境，避免與其他 Python 專案產生套件版本衝突。

```bash
cd backend

# 建立虛擬環境
python -m venv venv

# 啟動虛擬環境
# Windows:
venv\Scripts\activate
# macOS/Linux:
# source venv/bin/activate

# 啟動後，命令提示字元前方會出現 (venv) 字樣

# 安裝依賴
pip install -r requirements.txt
```

**注意**：每次開啟新的終端機視窗，都需要先啟動虛擬環境：

```bash
cd backend
venv\Scripts\activate   # Windows
# source venv/bin/activate  # macOS/Linux
```

**可選依賴（音檔處理）：**

#### Windows 安裝 FFmpeg

1. 前往 https://github.com/BtbN/FFmpeg-Builds/releases
2. 下載 `ffmpeg-master-latest-win64-gpl.zip`
3. 解壓縮到 `C:\ffmpeg`（或其他位置）
4. 將 FFmpeg 加入系統 PATH：
   - 按 `Win + R`，輸入 `sysdm.cpl`，按 Enter
   - 點選「進階」分頁 → 「環境變數」
   - 在「系統變數」中找到 `Path`，點選「編輯」
   - 點選「新增」，輸入 `C:\ffmpeg\bin`
   - 按「確定」儲存所有視窗
5. 開啟新的命令提示字元，輸入 `ffmpeg -version` 驗證安裝

#### macOS 安裝 FFmpeg

```bash
brew install ffmpeg
```

#### Linux 安裝 FFmpeg

```bash
sudo apt install ffmpeg
```

#### 本地 Whisper（離線語音轉文字）- 可選

> **大多數使用者不需要安裝此套件**，可使用線上服務（Groq Whisper 或 OpenAI Whisper API）。
>
> 僅在需要**完全離線**使用語音轉文字時才安裝。安裝需下載約 **3GB** 套件（包含 PyTorch）。

```bash
# 僅在需要離線功能時安裝
pip install openai-whisper
```

---

### 步驟 3：啟動後端伺服器

```bash
cd backend

# 確保虛擬環境已啟動（前方應顯示 (venv)）
venv\Scripts\activate   # Windows
# source venv/bin/activate  # macOS/Linux

# 啟動伺服器
python app.py
```

後端將在 **http://localhost:5000** 運行

---

### 步驟 4：安裝前端依賴

開啟**新的終端機視窗**（前端使用 npm，不需要 Python 虛擬環境）：

```bash
cd frontend
npm install
npm install -D @tailwindcss/typography
```

---

### 步驟 5：啟動前端開發伺服器

```bash
cd frontend
npm run dev
```

前端將在 **http://localhost:3000** 運行

---

## 使用方式

1. 開啟瀏覽器訪問 `http://localhost:3000`
2. 點擊「新增資料夾」建立資料夾來組織筆記本
3. 點擊「新增筆記本」建立第一個筆記本
4. 拖曳筆記本到資料夾中進行分類
5. 在左側面板新增來源（上傳文件、音檔、貼上網址、YouTube 連結或文字）
6. 在中間對話區與 AI 互動，AI 會根據來源內容回答
7. 在右側工作室生成摘要、心智圖、學習卡、測驗、Podcast 等

### 資料夾管理功能

- **建立資料夾**：自訂名稱和表情符號圖示
- **拖放整理**：拖曳筆記本到不同資料夾
- **展開/收合**：點擊資料夾標題切換顯示
- **編輯/刪除**：修改資料夾名稱或刪除（筆記本移至未分類）

---

## AI 提供商

| 提供商 | 特色 | 嵌入向量 | 圖片生成 | 需要 API Key |
|--------|------|:--------:|:--------:|:------------:|
| **Gemini** | Google 多模態模型 | ✅ | ✅ | 是 |
| **OpenAI** | GPT-4.1/5.1 + DALL-E 3 | ✅ | ✅ | 是 |
| **Anthropic** | Claude Opus/Sonnet 長上下文 | ❌* | ❌ | 是 |
| **Ollama** | 本地模型，免費無限使用 | ✅ | ❌ | 否 |
| **Groq** | 超高速推理 | ❌* | ❌ | 是 |
| **DeepSeek** | R1 推理模型，深度思考 | ❌* | ❌ | 是 |

> *不支援嵌入向量的提供商會自動使用備援提供商（Gemini/OpenAI/Ollama）

---

## 專案結構

```
專案_複刻NotebookLM/
├── backend/                    # Flask 後端 (Python)
│   ├── app.py                 # 應用入口
│   ├── config.py              # 配置管理
│   ├── requirements.txt       # Python 依賴
│   ├── models/                # 資料庫模型
│   ├── services/              # 商業邏輯服務
│   │   ├── ai_providers/      # AI Provider 抽象層
│   │   │   ├── gemini_provider.py
│   │   │   ├── openai_provider.py
│   │   │   ├── anthropic_provider.py
│   │   │   ├── ollama_provider.py
│   │   │   ├── groq_provider.py
│   │   │   └── deepseek_provider.py
│   │   ├── ai_service_manager.py  # AI 服務管理器
│   │   ├── rag_service.py     # RAG 檢索服務
│   │   ├── search_service.py  # 全文搜尋服務 (FTS5)
│   │   ├── audio_service.py   # STT/TTS 服務
│   │   ├── podcast_service.py # Podcast 生成
│   │   ├── youtube_service.py # YouTube 處理
│   │   ├── web_scraper_service.py  # 網頁擷取
│   │   ├── file_parser_service.py  # 文件解析
│   │   └── studio_service.py  # 工作室輸出生成
│   └── controllers/           # API 端點控制器
│
├── frontend/                   # React 前端 (TypeScript)
│   ├── src/
│   │   ├── pages/             # 頁面元件
│   │   ├── components/        # UI 元件
│   │   ├── store/             # Zustand 狀態管理
│   │   ├── api/               # API 客戶端
│   │   └── types/             # TypeScript 型別定義
│   ├── package.json
│   └── vite.config.ts
│
├── uploads/                    # 上傳檔案儲存目錄
├── .env.example               # 環境變數範本
└── 啟動指南.md                 # 本文件
```

---

## API 端點一覽

| 模組 | 端點 | 方法 | 說明 |
|------|------|------|------|
| **筆記本** | `/api/notebooks` | GET | 取得所有筆記本 |
| | `/api/notebooks` | POST | 建立筆記本 |
| | `/api/notebooks/:id` | GET | 取得單一筆記本 |
| | `/api/notebooks/:id` | DELETE | 刪除筆記本 |
| **資料夾** | `/api/folders` | GET | 取得所有資料夾 |
| | `/api/folders` | POST | 建立資料夾 |
| | `/api/folders/:id` | PUT | 更新資料夾 |
| | `/api/folders/:id` | DELETE | 刪除資料夾 |
| | `/api/folders/with-notebooks` | GET | 取得資料夾及筆記本 |
| | `/api/folders/:id/notebooks/:notebookId` | PUT | 將筆記本加入資料夾 |
| **來源** | `/api/notebooks/:id/sources` | GET | 取得來源列表 |
| | `/api/notebooks/:id/sources/upload` | POST | 上傳文件/音檔 |
| | `/api/notebooks/:id/sources/url` | POST | 新增網址 |
| | `/api/notebooks/:id/sources/youtube` | POST | 新增 YouTube |
| | `/api/notebooks/:id/sources/text` | POST | 新增文字 |
| | `/api/notebooks/:id/search` | POST | 搜尋來源內容 |
| | `/api/sources/reindex` | POST | 重建搜尋索引 |
| **對話** | `/api/notebooks/:id/chats` | GET | 取得對話歷史 |
| | `/api/notebooks/:id/chats/stream` | POST | 串流對話 (SSE) |
| | `/api/notebooks/:id/suggested-questions` | GET | 建議問題 |
| **工作室** | `/api/notebooks/:id/studio/summary` | POST | 生成摘要 |
| | `/api/notebooks/:id/studio/mindmap` | POST | 生成心智圖 |
| | `/api/notebooks/:id/studio/flashcards` | POST | 生成學習卡 |
| | `/api/notebooks/:id/studio/quiz` | POST | 生成測驗 |
| | `/api/notebooks/:id/studio/report` | POST | 生成報告 |
| | `/api/notebooks/:id/studio/presentation` | POST | 生成簡報（含配圖） |
| | `/api/notebooks/:id/studio/infographic` | POST | 生成資訊圖表（含配圖） |
| | `/api/notebooks/:id/studio/podcast` | POST | 生成 Podcast |
| | `/api/notebooks/:id/studio/podcast/script` | POST | 僅生成 Podcast 腳本 |
| | `/api/notebooks/:id/studio/podcast/audio` | POST | 從腳本生成音訊 |
| | `/api/notebooks/:id/studio/podcast/voices` | GET | 取得可用語音列表 |
| | `/api/notebooks/:id/studio/tts` | POST | 文字轉語音 |
| **設定** | `/api/settings` | GET | 取得設定 |
| | `/api/settings` | PUT | 更新設定 |
| | `/api/settings/providers` | GET | 取得可用提供商列表 |
| | `/api/settings/models/:provider` | GET | 取得提供商模型列表 |
| | `/api/settings/test-api` | POST | 測試 API 連線 |

---

## 支援的來源類型

| 類型 | 說明 | 檔案格式 |
|------|------|----------|
| PDF | PDF 文件 | `.pdf` |
| Text | 純文字文件 | `.txt`, `.md` |
| Word | Word 文件 | `.docx`, `.doc` |
| Excel | 試算表 | `.xlsx`, `.xls`, `.csv` |
| Web | 網頁連結 | URL |
| YouTube | YouTube 影片字幕/轉錄 | YouTube URL |
| **音檔** | 語音轉文字 (STT) | `.mp3`, `.wav`, `.m4a`, `.ogg`, `.flac`, `.webm` |

### 音檔處理 (STT 語音轉文字)

支援三種語音轉文字提供商：

| 提供商 | 類型 | 優點 | 需要 |
|--------|------|------|------|
| **OpenAI Whisper** | 線上 | 高品質，支援多語言 | OpenAI API Key |
| **Groq Whisper** | 線上 | 超高速，有免費額度 | Groq API Key |
| **本地 Whisper** | 離線 | 完全離線使用 | 安裝 3GB 套件 |

#### 使用方式

1. 在左側「來源」面板點擊「新增來源」
2. 選擇「音檔」分頁
3. 選擇 STT 提供商（**推薦使用 Groq Whisper**，免費又快）
4. 選擇語言
5. 上傳音檔

#### 推薦方案

- **Groq Whisper**（推薦）：免費額度、超高速，申請 API Key：https://console.groq.com/keys
- **OpenAI Whisper**：高品質，需要 OpenAI API Key
- **本地 Whisper**：需下載約 3GB 套件（PyTorch 等），僅適合需要離線使用的情況

> **提示**：大多數使用者只需要線上服務，不需要安裝本地 Whisper。

---

## 工作室輸出類型

| 類型 | 說明 | 特色功能 |
|------|------|----------|
| 語音摘要 | 來源內容的語音摘要 | - |
| 影片摘要 | 來源內容的影片摘要 | - |
| 心智圖 | Mermaid 視覺化圖表 | 匯出 SVG、縮放控制 |
| 報告 | 結構化的報告文件 | - |
| 學習卡 | 問答形式的學習卡片 | 🆕 Bloom 認知層次、難度分級 |
| 測驗 | 多元題型測驗 | 🆕 5 種題型、難度分級 |
| 資訊圖表 | Chart.js 數據視覺化 | 🆕 7 種圖表類型 |
| 簡報 | 簡報大綱 + 每頁配圖 | ✅ AI 配圖、PPTX 匯出 |
| 資料表 | 結構化資料表格 | - |
| **Podcast** | 多人對話播客（1-4 人） | 多種風格、TTS 語音 |

### 🆕 學習卡/測驗難度分級

生成學習卡和測驗時，可選擇難度：

| 難度 | Bloom 認知層次 | 說明 |
|------|---------------|------|
| 簡單 | 記憶、理解 | 基礎定義、事實回憶 |
| 中等 | 應用、分析 | 概念應用、比較分析 |
| 困難 | 評估、創造 | 批判思考、問題解決 |
| 混合 | 全部層次 | 循序漸進，包含各難度 |

### 🆕 測驗支援題型

| 題型 | 說明 |
|------|------|
| 選擇題 | 單選題，A/B/C/D 選項 |
| 判斷題 | 是非題，正確/錯誤 |
| 填空題 | 填入正確答案 |
| 配對題 | 左右項目配對 |
| 簡答題 | 開放式問答 |

### 🆕 資訊圖表 (Chart.js)

自動根據資料選擇最適合的圖表類型：

| 圖表類型 | 用途 |
|----------|------|
| Bar（長條圖） | 類別比較 |
| Line（折線圖） | 趨勢變化 |
| Pie（圓餅圖） | 佔比分布 |
| Doughnut（甜甜圈圖） | 佔比分布（變體） |
| Radar（雷達圖） | 多維度比較 |
| Scatter（散佈圖） | 相關性分析 |
| PolarArea（極區圖） | 類別比較（變體） |

### Podcast 生成

支援生成多人對話播客：
- **講者人數**: 1-4 人
- **對談風格**: 輕鬆對談、教育講解、辯論討論、專訪形式
- **時長**: 5/10/15/20 分鐘
- **音訊生成**: 可選擇將腳本轉為語音（TTS）

### 圖片生成說明

簡報和資訊圖表功能支援 AI 自動配圖：

- **Gemini**: 使用 `gemini-2.0-flash-exp-image-generation` 多模態模型生成圖片（標準 API Key 支援）
- **OpenAI**: 使用 `dall-e-3` 模型生成圖片

> **注意**：Gemini 使用多模態模型（非 Imagen），可直接用標準 API Key 生成圖片。

---

## 搜尋功能

支援三種搜尋模式：

| 模式 | 說明 | 特點 |
|------|------|------|
| **全文搜尋** | SQLite FTS5 全文索引 | 快速、精確關鍵字匹配 |
| **向量搜尋** | 語意相似度搜尋 | 理解語意、找相關內容 |
| **混合搜尋** | 全文 + 向量結合 | 最佳結果，推薦使用 |

### 🆕 進階 RAG 搜尋技術

| 技術 | 說明 |
|------|------|
| **RRF 融合** | Reciprocal Rank Fusion 演算法結合全文+向量搜尋排名 |
| **Query Expansion** | LLM 自動擴展查詢，增加語意變體 |
| **LLM Reranking** | 使用 LLM 對搜尋結果智能重排序 |
| **去重機制** | 自動合併重複的搜尋結果 |

---

## 常見問題

### Q: 後端啟動時出現 ModuleNotFoundError？
**A:** 確保已在虛擬環境中執行 `pip install -r requirements.txt` 安裝所有依賴。

### Q: 安裝依賴時出現版本衝突（dependency conflicts）？
**A:** 這是因為你的 Python 環境中有其他專案的套件衝突。解決方法：
1. 建立新的虛擬環境：
   ```bash
   cd backend
   python -m venv venv
   venv\Scripts\activate   # Windows
   pip install -r requirements.txt
   ```
2. 確保每次都在虛擬環境中執行

### Q: 前端無法連接後端？
**A:**
1. 確認後端正在 `http://localhost:5000` 運行
2. 檢查 `vite.config.ts` 中的 proxy 設定是否正確

### Q: API Key 無效？
**A:**
1. 確認 `.env` 檔案位於 `backend/` 目錄中
2. 確認 API Key 正確且有效
3. 在設定頁面使用「測試連線」功能驗證

### Q: 上傳檔案失敗？
**A:**
1. 確認 `uploads/` 目錄存在且有寫入權限
2. 檔案大小限制為 50MB

### Q: 音檔轉錄失敗？
**A:**
1. 確認已安裝 FFmpeg（用於格式轉換）
2. 音檔大小限制為 25MB
3. 嘗試使用不同的 STT 提供商

### Q: Ollama 無法連線？
**A:**
1. 確認 Ollama 服務已啟動：`ollama serve`
2. 確認模型已下載：`ollama pull llama3.2`
3. 確認 `OLLAMA_BASE_URL` 設定正確

---

## 技術棧

| 層級 | 技術 |
|------|------|
| 後端框架 | Flask 3.0 |
| 資料庫 | SQLite + SQLAlchemy + FTS5 |
| 前端框架 | React 18 + TypeScript |
| 建置工具 | Vite |
| 樣式 | TailwindCSS |
| 狀態管理 | Zustand |
| 圖表渲染 | Chart.js + react-chartjs-2 |
| 心智圖渲染 | Mermaid.js |
| AI 文字 API | Gemini / OpenAI / Anthropic / Ollama / Groq / DeepSeek |
| AI 圖片 API | Gemini 多模態 / DALL-E 3 |
| 語音轉文字 | OpenAI Whisper / Groq Whisper / 本地 Whisper |
| 文字轉語音 | OpenAI TTS / Google Cloud TTS |

---

## 開發指令

```bash
# 後端開發模式（自動重載）
cd backend
flask run --debug

# 前端開發模式
cd frontend
npm run dev

# 前端建置生產版本
cd frontend
npm run build

# 前端預覽生產版本
cd frontend
npm run preview
```

---

## 授權

本專案僅供學習和個人使用。
