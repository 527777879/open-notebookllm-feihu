# NoteBookLLM 開發記錄 - 2026-01-10

## 本次更新摘要

### 四大功能升級

1. **RAG 混合搜尋升級** - 進階搜尋演算法
2. **學習卡/測驗升級** - Bloom 認知層次 + 難度分級
3. **資訊圖表升級** - Chart.js 數據視覺化
4. **Draw.io 流程圖/架構圖** - react-drawio 整合

---

## 新增功能詳細說明

### 1. RAG 混合搜尋升級

| 技術 | 說明 |
|------|------|
| **RRF 融合** | Reciprocal Rank Fusion 演算法，結合全文+向量搜尋排名 |
| **Query Expansion** | LLM 自動擴展查詢語意，生成同義詞和相關詞 |
| **LLM Reranking** | 使用 LLM 對搜尋結果智能重排序 |
| **去重機制** | 自動合併重複的搜尋結果 |

#### 更新檔案
- `backend/services/search_service.py` - 新增 RRF、Query Expansion、Reranking 方法

### 2. 學習卡/測驗升級

#### Bloom 認知層次

| 層次 | 說明 |
|------|------|
| 記憶 (Remember) | 回憶定義、術語、事實 |
| 理解 (Understand) | 解釋概念、比較異同 |
| 應用 (Apply) | 實際情境應用 |
| 分析 (Analyze) | 拆解結構、找出關聯 |
| 評估 (Evaluate) | 批判思考、判斷優劣 |
| 創造 (Create) | 整合知識、設計方案 |

#### 難度分級

| 難度 | 包含層次 |
|------|----------|
| 簡單 (easy) | 記憶、理解 |
| 中等 (medium) | 應用、分析 |
| 困難 (hard) | 評估、創造 |
| 混合 (mixed) | 全部層次 |

#### 測驗題型

| 題型 | 說明 |
|------|------|
| multiple_choice | 單選題 |
| true_false | 判斷題 |
| fill_blank | 填空題 |
| matching | 配對題 |
| short_answer | 簡答題 |

#### 更新檔案
- `backend/services/prompts.py` - 更新 FLASHCARDS、QUIZ 提示詞
- `backend/services/studio_service.py` - 新增 difficulty 參數
- `backend/controllers/studio_controller.py` - 新增難度 API 參數
- `frontend/src/components/studio/StudioPanel.tsx` - 新增難度選擇 UI

### 3. 資訊圖表升級 (Chart.js)

#### 支援圖表類型

| 類型 | 用途 |
|------|------|
| bar | 長條圖 - 類別比較 |
| line | 折線圖 - 趨勢變化 |
| pie | 圓餅圖 - 佔比分布 |
| doughnut | 甜甜圈圖 - 佔比分布變體 |
| radar | 雷達圖 - 多維度比較 |
| scatter | 散佈圖 - 相關性分析 |
| polarArea | 極區圖 - 類別比較變體 |

#### 更新檔案
- `backend/services/prompts.py` - 新增 INFOGRAPHIC 提示詞（輸出 Chart.js JSON）
- `backend/services/studio_service.py` - 更新 generate_infographic 方法
- `frontend/package.json` - 新增 chart.js、react-chartjs-2 依賴
- `frontend/src/components/studio/ChartRenderer.tsx` - **新增**，Chart.js 渲染組件
- `frontend/src/types/index.ts` - 新增 InfographicData、ChartConfig 型別

### 4. Draw.io 流程圖/架構圖

#### 技術說明

| 技術 | 說明 |
|------|------|
| **react-drawio** | Draw.io 嵌入式 React 組件 |
| **mxGraph XML** | Draw.io 圖形格式，使用 mxCell 元素定義形狀和連線 |
| **AI 自動生成** | LLM 根據內容自動生成 Draw.io XML |

#### 支援圖表類型

| 類型 | 說明 |
|------|------|
| flowchart | 流程圖 - 步驟流程、決策分支 |
| architecture | 架構圖 - 系統組件、服務關係 |
| sequence | 時序圖 - 互動順序 |
| class | 類別圖 - OOP 結構 |
| er | ER 圖 - 資料庫關係 |
| network | 網路圖 - 網路拓撲 |

#### Draw.io 形狀元素

| 形狀 | style 屬性 | 用途 |
|------|-----------|------|
| 矩形 | `shape=rectangle` | 一般步驟、模組 |
| 菱形 | `shape=rhombus` | 條件判斷 |
| 橢圓 | `ellipse` | 開始/結束 |
| 圓柱 | `shape=cylinder` | 資料庫 |
| 雲朵 | `shape=cloud` | 外部服務 |

#### 更新檔案
- `backend/services/prompts.py` - 新增 FLOWCHART、DIAGRAM 提示詞
- `backend/services/studio_service.py` - 新增 generate_flowchart、generate_diagram 方法
- `backend/controllers/studio_controller.py` - 新增流程圖/架構圖 API 端點
- `frontend/package.json` - 新增 react-drawio 依賴
- `frontend/src/components/studio/DiagramRenderer.tsx` - **新增**，Draw.io 渲染組件
- `frontend/src/types/index.ts` - 新增 DiagramData、DiagramType 型別
- `frontend/src/api/studio.ts` - 新增 generateFlowchart、generateDiagram API
- `frontend/src/components/studio/StudioPanel.tsx` - 整合流程圖/架構圖功能

---

## 更新的檔案清單

### 後端更新

| 檔案 | 更新內容 |
|------|----------|
| `backend/services/search_service.py` | RRF 融合、Query Expansion、LLM Reranking |
| `backend/services/prompts.py` | FLASHCARDS、QUIZ、INFOGRAPHIC、FLOWCHART、DIAGRAM 提示詞 |
| `backend/services/studio_service.py` | difficulty 參數、Chart.js 輸出、流程圖/架構圖生成 |
| `backend/controllers/studio_controller.py` | 難度分級 API、流程圖/架構圖 API 端點 |

### 前端更新

| 檔案 | 更新內容 |
|------|----------|
| `frontend/package.json` | 新增 chart.js、react-chartjs-2、react-drawio |
| `frontend/src/types/index.ts` | DifficultyLevel、InfographicData、ChartConfig、DiagramData 型別 |
| `frontend/src/api/studio.ts` | difficulty 參數、StudioOutputOptions、流程圖/架構圖 API |
| `frontend/src/components/studio/ChartRenderer.tsx` | **新增** - Chart.js 渲染組件 |
| `frontend/src/components/studio/DiagramRenderer.tsx` | **新增** - Draw.io 渲染組件 |
| `frontend/src/components/studio/StudioPanel.tsx` | 難度選擇 Modal、Chart.js、Draw.io 整合 |
| `frontend/src/components/studio/MindmapRenderer.tsx` | 修復未使用變數警告 |

### 文件更新

| 檔案 | 更新內容 |
|------|----------|
| `README.md` | 新增功能說明、技術棧更新 |
| `啟動指南.md` | 工作室功能、搜尋技術更新 |
| `開發記錄_2026-01-10.md` | 本檔案 |

---

## 新增依賴套件

### npm (前端)

```json
{
  "chart.js": "^4.4.7",
  "react-chartjs-2": "^5.3.0",
  "react-drawio": "^latest"
}
```

---

## API 變更

### 學習卡 API

```
POST /api/notebooks/:id/studio/flashcards
Body: {
  "count": 10,
  "source_ids": [...],
  "difficulty": "easy" | "medium" | "hard" | "mixed"
}
```

### 測驗 API

```
POST /api/notebooks/:id/studio/quiz
Body: {
  "count": 10,
  "source_ids": [...],
  "difficulty": "easy" | "medium" | "hard" | "mixed"
}
```

### 資訊圖表 API

```
POST /api/notebooks/:id/studio/infographic
Body: {
  "source_ids": [...],
  "with_ai_image": false
}
Response: {
  "charts": [
    {
      "id": "chart-1",
      "title": "圖表標題",
      "type": "bar",
      "config": { /* Chart.js 配置 */ },
      "insights": ["洞察1", "洞察2"]
    }
  ],
  "summary": {
    "key_findings": [...],
    "recommendations": [...]
  }
}
```

### 流程圖 API

```
POST /api/notebooks/:id/studio/flowchart
Body: {
  "source_ids": [...]
}
Response: {
  "title": "流程圖標題",
  "description": "說明",
  "xml": "<mxGraphModel>...</mxGraphModel>",
  "type": "flowchart"
}
```

### 架構圖 API

```
POST /api/notebooks/:id/studio/diagram
Body: {
  "source_ids": [...],
  "diagram_type": "auto" | "architecture" | "sequence" | "class" | "er" | "network"
}
Response: {
  "title": "架構圖標題",
  "description": "說明",
  "xml": "<mxGraphModel>...</mxGraphModel>",
  "type": "diagram",
  "diagram_type": "architecture"
}
```

---

## 專案狀態

### 已完成功能

- [x] 筆記本 CRUD
- [x] 資料夾管理（含拖放）
- [x] 來源管理（文件、網址、YouTube、文字、音檔）
- [x] AI 對話（RAG + SSE 串流）
- [x] 6 種 AI 提供商
- [x] **進階 RAG 搜尋**（RRF + Query Expansion + Reranking）
- [x] **學習卡/測驗難度分級**（Bloom 認知層次）
- [x] **Chart.js 資訊圖表**（7 種圖表類型）
- [x] **Draw.io 流程圖/架構圖**（react-drawio 整合）
- [x] Podcast 播客生成
- [x] 簡報生成（含 AI 配圖、PPTX 匯出）
- [x] 心智圖生成（Mermaid 渲染）

---

## 下次繼續事項

- [ ] 測試所有新功能
- [ ] 效能優化
- [ ] 使用者體驗改善
